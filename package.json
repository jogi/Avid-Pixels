{
  "name": "avid-pixels",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": ".venv/bin/python3 _misc/extract_exif.py",
    "build": "npm run prebuild && hugo",
    "build:preview": "npm run prebuild && hugo --buildDrafts --buildFuture",
    "server": "hugo server"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "postcss-cli": "^11.0.0"
  },
  "browserslist": [
    "last 3 versions",
    "> 1%",
    "not dead"
  ]
}
